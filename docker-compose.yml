version: '3.8'
name: ${KIRA_INSTANCE_NAME:-kira-mind-bot}

# Ротация логов: макс. 10 МБ на файл, 3 файла (~30 МБ на сервис)
x-logging: &default-logging
  driver: json-file
  options:
    max-size: "10m"
    max-file: "3"

x-restart: &default-restart
  restart: unless-stopped

services:
  postgres:
    image: postgres:16-alpine
    <<: *default-restart
    environment:
      POSTGRES_USER: ${DB_USER:-postgres}
      POSTGRES_PASSWORD: ${DB_PASSWORD}
      POSTGRES_DB: ${DB_NAME:-KiraMind}
    volumes:
      - postgres_data:/var/lib/postgresql/data
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U ${DB_USER:-postgres}"]
      interval: 10s
      timeout: 5s
      retries: 5
    logging: *default-logging

  qdrant:
    image: qdrant/qdrant:latest
    <<: *default-restart
    volumes:
      - qdrant_storage:/qdrant/storage
    logging: *default-logging

  kira-mind-bot:
    build:
      context: ./kira-mind-bot
      args:
        NODE_ENV: ${NODE_ENV:-production}
    container_name: ${KIRA_INSTANCE_NAME:-kira-mind-bot}
    <<: *default-restart
    env_file:
      - ./kira-mind-bot/.env.production
    environment:
      - NODE_ENV=${NODE_ENV:-production}
      - ASSISTANT_PROFILE=KiraMindBot
      - PERSONALITY_FILE=/app/personality/personality.json
    volumes:
      - ./personality.json:/app/personality/personality.json:ro
    depends_on:
      postgres:
        condition: service_healthy
      qdrant:
        condition: service_started
    logging: *default-logging

  admin-panel:
    build:
      context: .
      dockerfile: admin-panel/Dockerfile
    <<: *default-restart
    ports:
      - "${ADMIN_PORT:-8080}:3000"
    volumes:
      - ./kira-mind-bot/.env.production:/app/env/bot.env
      - ./personality.json:/app/personality/personality.json
      - /var/run/docker.sock:/var/run/docker.sock:ro
    environment:
      - ADMIN_USERNAME=${ADMIN_USERNAME:-admin}
      - ADMIN_PASSWORD=${ADMIN_PASSWORD:-changeme}
      - BOT_ENV_FILE=/app/env/bot.env
      - PERSONALITY_FILE=/app/personality/personality.json
      - KIRA_RUNTIME_HEALTH_URL=http://kira-mind-bot:3100
      - KIRA_BOT_CONTAINER_NAME=${KIRA_INSTANCE_NAME:-kira-mind-bot}
    logging: *default-logging

volumes:
  postgres_data:
    name: ${KIRA_INSTANCE_NAME:-kira-mind-bot}_postgres_data
    driver: local
  qdrant_storage:
    name: ${KIRA_INSTANCE_NAME:-kira-mind-bot}_qdrant_storage
    driver: local
